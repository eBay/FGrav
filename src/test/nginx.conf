server {
  listen 80;
  location / {
    index  index.html;
  }
  location /collapsed {
    autoindex on;
  }
  location /js/color {
    autoindex on;
  }
  location /js/frame {
    autoindex on;
  }
}
